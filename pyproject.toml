[build-system]
requires = [
    "wheel",
    "setuptools >= 64",
    "cython >= 3.0.10",
    "numpy >= 1.26.4",
    "cmdstanpy >= 1.2.5"
]
build-backend = "setuptools.build_meta"

[project]
name = "pyrethink"
authors = [{name = "Julien Lerat, CSIRO Environment", email="julien.lerat@csiro.au"}]
description = "Tool supporting the workflows"
readme = "README.md"
license = {file = "LICENSE"}
keywords = ["data processing", "rainfall", "streamflow", "rainfall-runoff", "hydrology"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python 3",
    "Programming Language :: C",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Scientific/Engineering :: Hydrology"
]    
requires-python = ">= 3.8"
dependencies = [
    "cython >= 3.0.10",
    "numpy >= 1.26.4",
    "cmdstanpy >= 1.2.5",
    "scipy >= 1.13.0",
    "pandas >= 2.0.0"
]
dynamic = ["version"]

#[project.urls]
#Homepage = "https://github.com/csiro-hydroinformatics/hydrodiy"
#Repository = "https://github.com/csiro-hydroinformatics/hydrodiy"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "pyrethink.__version__"}
